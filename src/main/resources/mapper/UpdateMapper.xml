<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.VisaAPI.mapper.UpdateMapper">
    <update id="updateuser" parameterType="com.example.VisaAPI.model.UpdateModel" >
        UPDATE visa_user_api_role SET password = #{newpassword} where username=#{username} AND password=#{password}  AND status='ACTIVE';
    </update>
    <select id="selectuser" resultType="com.example.VisaAPI.model.UpdateModel">
        SELECT username,password FROM visa_user_api_role where username=#{username} AND password=#{password} AND status='ACTIVE';
    </select>
</mapper>	
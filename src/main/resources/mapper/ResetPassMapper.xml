<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.VisaAPI.mapper.ResetPassMapper">

    <select id="selectuser" resultType="com.example.VisaAPI.model.ResetPassModel" >
    SELECT username
	FROM visa_user_api
	WHERE username = #{username} and visa_id = #{visa_id} and birthday=#{birthdayDate} AND status='ACTIVE';
    </select>
    <update id="resetpass" parameterType="com.example.VisaAPI.model.ResetPassModel" >
    UPDATE visa_user_api_role SET password = #{newpassword}
	WHERE username = #{username} AND status='ACTIVE';
    </update>

</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.VisaAPI.mapper.MypageMapper">

	<select id="SelectByUsername" resultType="com.example.VisaAPI.model.MypageModel">
		SELECT    id,username,visa_id,name,birthday,sex,country,address,visa_date,visa_type
		FROM visa_user_api WHERE username = #{username} AND status = 'ACTIVE'
	</select>
</mapper>